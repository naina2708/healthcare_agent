version: '3.8'

services:
  healthcare-agent:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - .:/app
    command: uvicorn backend.server:app --host 0.0.0.0 --port 8000 --reload